module.exports={
    publicPath:'./', // La ruta para implementar el paquete de la aplicación
    outputDir:'dist', // El directorio de salida al empaquetar y construir
    devServe:{
        port:8080,
    }
}
